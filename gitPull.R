#cd git_environment
#cd MB12
#git remote add upstream http://github.com/16EAGLE/MB12.git
# git fetch upstream

#git checkout master
#git rebase upstream/master
#git push -f origin master
#git commit -m "before rebase"

#git add .
#git commit -m "before rebase"
#git push origin master
#git checkout master
#git rebase upstream/master
#git remote add upstream http://github.com/16EAGLE/MB12.git
#git rebase upstream/master